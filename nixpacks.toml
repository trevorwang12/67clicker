[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmd = "npm ci --only=production"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run start"